#!/usr/bin/python3


def divisible_by_2(my_list=[]):

    size = len(my_list)
    if size != 0:
        bol_list = []
        for i in my_list:
            if (i % 2) == 0:
                new_list += True
            else:
                new_list += False

        return bol_list
