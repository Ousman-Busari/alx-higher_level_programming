#!/usr/bin/python3

def remove_char_at(str, n):
    str_cpy = ""
    for i in range(len(str)):
        if (i < n):
            str_cpy[i] = str[i]
        elif (i > n):
            str_cpy[i - 1] = str[i]
